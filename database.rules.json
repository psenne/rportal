{
    "rules": {
        ".read": "auth.token.email.matches(/.*@renegadetec.com$/) || auth.token.email.matches(/.*@renxtech.com$/) || auth.token.email.matches(/.*@buchanan[-]edwards.com$/)",
        ".write": "auth.token.email.matches(/.*@renegadetec.com$/) || auth.token.email.matches(/.*@renxtech.com$/) || auth.token.email.matches(/.*@buchanan[-]edwards.com$/)",
        "auditing": {
            ".indexOn": ["eventdate"]
        },
        "users": {
            ".indexOn": ["email"]
        }
    }
}
